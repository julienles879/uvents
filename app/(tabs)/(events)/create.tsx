import { ThemedText } from '@/components/ThemedText';
import { ThemedView } from '@/components/ThemedView';

export default function eventCreateScreen() {
    return (
        <ThemedView style={{ flex: 1 }} >
            <ThemedText>Create Event Screen</ThemedText>
        </ThemedView>
    );
}
